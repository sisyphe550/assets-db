# FAMS 前端页面详细设计

> 对应后端 API 契约 `backend/doc/03-api-contract.md`

---

## 1. 登录页

**路由**：`/login`

**组件状态**：

```
┌──────────────────────────┐
│                          │
│      高校固定资产管理      │
│         系统              │
│                          │
│  ┌──────────────────────┐│
│  │ 用户名               ││
│  │ [_______________]    ││
│  │ 密码                 ││
│  │ [_______________]    ││
│  │                      ││
│  │ [      登  录      ] ││
│  └──────────────────────┘│
│                          │
└──────────────────────────┘
```

**交互流程**：
1. 输入用户名密码 → 点击登录 → `POST /user/login`
2. **成功（code=0）**：
   - `accessToken` + `refreshToken` 存 localStorage
   - `dispatch(setCredentials(...))` 更新 Redux store
   - `GET /user/me` 获取用户信息
   - 根据 `roleLevel` 跳转：1→`/admin/dashboard`、2→`/college/dashboard`、3→`/user/assets`
3. **失败（code=40101）**：表单下方显示 `message` 错误提示
4. **已禁用（code=40301）**：提示"账户已禁用，请联系管理员"

**状态管理**：
- 表单：本地 `useState`（不需要全局 store）
- 登录结果：Redux `authSlice`（全局共享）

---

## 2. 仪表盘

**路由**：`/admin/dashboard` 或 `/college/dashboard`

**页面组成**：

```
┌─────────────────────────────────────────────────────┐
│  仪表盘                                              │
│                                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ 资产总数  │ │ 在库资产  │ │ 领用中    │ │ 待审批  │ │
│  │   120    │ │    80    │ │    30    │ │    5    │ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────┘ │
│                                                      │
│  ┌─────────────────────┐ ┌─────────────────────────┐ │
│  │ 按类别资产分布（饼图）│ │ 审批趋势（折线图）       │ │
│  └─────────────────────┘ └─────────────────────────┘ │
│                                                      │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 最近工单列表（前 10 条，点击跳转详情）            │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**API 调用**：
- 统计卡片：`GET /report/assets/by-dept`（校级全量，院级过滤本院）
- 饼图：`GET /report/assets/by-category`
- 待审批数：`GET /workflow/requests?scope=todo&status=1`

**差异**：校级显示全校统计，院级只显示本院统计。

---

## 3. 资产列表页

**路由**：`/admin/assets`、`/college/assets`

**页面组成**：

```
┌─────────────────────────────────────────────────────┐
│  资产管理                    [+ 新增资产] [导出]      │
│                                                      │
│  筛选栏：                                            │
│  [类别▾] [状态▾] [关键词搜索________] [搜索]         │
│                                                      │
│  ┌───────────────────────────────────────────────┐   │
│  │ 编号 │名称│类别│价格│地点│状态│领用人│操作   │   │
│  │──────┼────┼────┼────┼────┼────┼──────┼──────│   │
│  │ 0001 │激光│设备│15w │101 │在库│  -   │详情  │   │
│  │ 0002 │3D  │设备│8w  │102 │领用│ 李同学│详情  │   │
│  │ ...  │... │... │... │... │... │ ...  │...   │   │
│  └───────────────────────────────────────────────┘   │
│                        第 1/6 页  [<] [1] [2] [>]     │
└─────────────────────────────────────────────────────┘
```

**API 调用**：`useGetAssetsQuery(params)` — RTK Query 自动管理 loading/分页/筛选

**筛选状态**：本地 `useState`，变化时更新 `params` 触发 refetch

**操作列**：
- "详情" → 跳转 `/admin/assets/:id`
- 校级额外显示"删除"按钮（`DELETE /assets/:id`，需二次确认）

---

## 4. 工单详情页（含审批操作）

**路由**：`/admin/workflow/:id`、`/college/workflow/:id`

**这是交互最复杂的页面**：

```
┌─────────────────────────────────────────────────────┐
│  工单详情                                  [#88001]  │
│                                                      │
│  ┌───────────────────────────────────────────────┐   │
│  │ 资产信息                                       │   │
│  │  资产编号: EQUIP-2026-0001  名称: 激光切割机    │   │
│  │  当前状态: 在库              地点: 一号实验楼101 │   │
│  └───────────────────────────────────────────────┘   │
│                                                      │
│  ┌───────────────────────────────────────────────┐   │
│  │ 申请信息                                       │   │
│  │  申请人: 李同学        类型: 领用              │   │
│  │  原因: 课程设计需要使用                          │   │
│  │  提交时间: 2026-07-07 10:00                     │   │
│  │  状态: 审批中 / 待院级初审                      │   │
│  └───────────────────────────────────────────────┘   │
│                                                      │
│  ┌───────────────────────────────────────────────┐   │
│  │ 审批历史（时间线）                              │   │
│  │  ● 2026-07-07 10:00  李同学  提交申请          │   │
│  │  ● 2026-07-07 10:30  王院管  院级初审同意       │   │
│  │    审批意见: 同意                                │   │
│  │  ○ 待校级复审                                    │   │
│  └───────────────────────────────────────────────┘   │
│                                                      │
│  ┌───────────────────────────────────────────────┐   │
│  │ [同意] [驳回]                    (仅当等待当前   │   │
│  │ [审批意见: _______________]      用户审批时显示) │   │
│  └───────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**审批按钮显示逻辑**：

| 当前用户角色 | stage | 显示按钮 |
|---|---|---|
| role=2 (院级管理员) | 1 (待院级初审) | 同意 + 驳回 |
| role=1 (校级管理员) | 2 (待校级复审) | 同意 + 驳回 |
| 其他情况 | — | 按钮隐藏 |

**交互细节**：
- 同意/驳回前弹出确认框，附带审批意见输入
- 终审通过后，页面的状态标签自动刷新（RTK Query `invalidatesTags`）

---

## 5. 创建工单页

**路由**：`/user/workflow/create`

```
┌─────────────────────────────────────────────────────┐
│  新建申请                                            │
│                                                      │
│  申请类型:                                           │
│  [领用] [归还] [报修] [报废]                          │
│                                                      │
│  选择资产:                                           │
│  [搜索资产编号或名称______________]                   │
│  ┌───────────────────────────────────────────────┐   │
│  │ (根据类型过滤可选资产列表，如领用只显示 status=1) │   │
│  │ ○ EQUIP-2026-0001 激光切割机  在库  一号实验楼101│   │
│  │ ○ EQUIP-2026-0002 3D打印机    在库               │   │
│  └───────────────────────────────────────────────┘   │
│                                                      │
│  申请原因:                                           │
│  ┌──────────────────────────────────────────────┐    │
│  │                                              │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  [提交申请]                                          │
└─────────────────────────────────────────────────────┘
```

**类型切换逻辑**：
- 领用 → 过滤 assets where status=1（在库）
- 归还 → 过滤 assets where userId=currentUser.id AND status=2（领用中 + 本人）
- 报修 → 过滤 assets where status IN (1,2)（在库或领用中）
- 报废 → 过滤 assets where status IN (1,3)（在库或维修中）

**提交反馈**：
- 40902："该资产已有审批中的工单，请选择其他资产"
- 42201：显示具体拒绝原因（如"资产当前不可领用"）

---

## 6. 盘点详情页（Univer 表格 + 差异报告）

**路由**：`/admin/inventory/tasks/:id`、`/college/inventory/tasks/:id`

**这是技术最复杂的页面**，分为三个阶段：

### 阶段 1：盘点进行中（task.status = 1）

```
┌─────────────────────────────────────────────────────┐
│  2026 年度信息学院实验室盘点       状态: 进行中       │
│  进度: 已提交 15 / 应盘 50 台                        │
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │           Univer 协同表格区域                  │    │
│  │ ┌──────┬──────────┬──────┬─────────┬──────┐  │    │
│  │ │资产编号│ 资产名称  │账面位置│ 实际位置 │ 备注 │  │    │
│  │ ├──────┼──────────┼──────┼─────────┼──────┤  │    │
│  │ │0001  │激光切割机 │101   │[_____]  │[____]│  │    │
│  │ │0002  │3D打印机  │102   │[_____]  │[____]│  │    │
│  │ │ ...  │ ...      │ ...  │ ...     │ ...  │  │    │
│  │ └──────┴──────────┴──────┴─────────┴──────┘  │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  [保存草稿]  [批量提交]                               │
│                                                      │
│  提交结果:                                            │
│  ✅ 成功: EQUIP-2026-0001, EQUIP-2026-0002           │
│  ❌ 冲突: EQUIP-2026-0003 (正在被他人盘点，请重试)     │
└─────────────────────────────────────────────────────┘
```

### 阶段 2：已归档（task.status = 2）

- Univer 表格变为只读
- 显示"比对中..."加载状态

### 阶段 3：已完成（task.status = 3）

```
┌─────────────────────────────────────────────────────┐
│  盘点已完成                                          │
│                                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│  │  ✅ 相符  │ │  ⚠️ 盘盈  │ │  ❌ 盘亏  │             │
│  │    30    │ │    2     │ │    3     │             │
│  └──────────┘ └──────────┘ └──────────┘             │
│                                                      │
│  差异明细表格（可筛选 diff_status）：                  │
│  ┌──────────────────────────────────────────────┐    │
│  │ 资产编号│名称│账面位置│实际位置│状态│操作    │    │
│  │──────────────────────────────────────────────│    │
│  │ UNKNOWN │-  │  -     │仓库    │盘盈│登记入账│    │
│  │ 0005    │车床│机械学院│未清查  │盘亏│-      │    │
│  └──────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**Univer 关键交互**：
1. 页面加载时调用 `GET /tasks/:id/expected-assets` 获取应盘资产，填入表格首三列（资产编号/名称/账面位置，设为只读）
2. 盘点员在"实际位置"和"备注"列填写，点击"保存草稿"或 Ctrl+S → `POST /tasks/:id/submit`
3. 提交后，根据响应中的 `conflicts` 列表将对应行标红（Ant Design `rowClassName`）
4. 冲突行可单独重新编辑提交
5. "批量提交"时传 `expectedUpdatedAt` 做 CAS 检查

---

## 7. 统计报表页

**路由**：`/admin/reports`

```
┌─────────────────────────────────────────────────────┐
│  统计报表                                            │
│                                                      │
│  [按部门] [按类别] [盘点差异]                          │
│                                                      │
│  ┌─────────────────────┐ ┌─────────────────────────┐ │
│  │ 各学院资产数量（柱状图）│ │ 资产类型分布（饼图）    │ │
│  └─────────────────────┘ └─────────────────────────┘ │
│                                                      │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 部门资产明细表                                    │ │
│  │ 部门 │ 总数 │ 在库 │ 领用中 │ 维修中 │ 总价值    │ │
│  │ 信息 │ 120  │ 80   │ 30    │ 5     │ 150万     │ │
│  │ 机械 │ 80   │ 60   │ 15    │ 3     │ 80万      │ │
│  └─────────────────────────────────────────────────┘ │
│                                                      │
│  [导出 CSV]                                          │
└─────────────────────────────────────────────────────┘
```

**导出交互**：
1. 点击"导出 CSV" → `POST /report/export { exportType: "asset_list" }` → 返回 `jobId`
2. 轮询 `GET /report/export/:jobId` 显示进度（0-排队 / 1-处理中 / 2-完成 / 3-失败）
3. status=2 → 自动触发下载 `GET /report/export/:jobId/download`

---

## 8. 跨页面数据流

```
资产列表页
  │ 点击"详情"
  ▼
资产详情页              创建工单页
  │                       │ 选择资产
  ▼                       ▼
  └────── 工单详情页 ──────┘
            │ 审批
            ▼
        工单列表刷新
            │
            ▼
        资产状态自动更新（Kafka consumer 异步 → RTK Query 失效）
```

**关键**：任何修改资产状态的操作（创建工单、审批通过）都会触发 RTK Query 的 `invalidatesTags: ['AssetList']`，确保资产列表和资产详情的缓存自动刷新。

---

## 9. 错误处理策略

| 层级 | 处理方式 |
|---|---|
| 网络错误 | `baseQuery` 中统一拦截，弹全局 notification |
| 40101/40102 | 自动尝试 refresh token，失败则跳转登录页 |
| 40301/40302 | 页面显示"无权访问"，不弹全局通知 |
| 40901/40902 | 在触发操作的组件内展示错误（如盘点冲突行标红、重复申请 alert） |
| 42201 | 显示 `message` 内容（后端返回具体的业务拒绝原因） |
| 50001/50301 | 全局 notification "系统异常，请稍后重试" |

---

## 10. 页面间路由过渡

- 所有页面切换使用 React Router `<Outlet />`，不需要额外的过渡动画
- 盘点详情页的 Univer 组件在 `React.lazy(() => import('./UniverSpreadsheet'))` 中懒加载（Univer 体积较大）
- 表单提交后使用 `useNavigate` 跳转到列表页（如创建资产成功后跳转 `/admin/assets`）

---

*文档版本：v1.0 | 2026-07-07*
